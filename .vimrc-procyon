if filereadable(expand("~/.vimrc-custom"))
    source ~/.vimrc-custom
endif

" neovim plugins

lua << EOF
require('gen').setup({
    model = "llama3.1",
    display_mode = "split"
})
vim.keymap.set({ 'n', 'v' }, '<Tab>', ':Gen<CR>')
EOF
